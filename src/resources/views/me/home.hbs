<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand" href="">Coursera</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div
      class="collapse navbar-collapse hidden-arrow"
      id="navbarSupportedContent"
    >
      <ul class="navbar-nav ms-auto">

        {{! notification }}
        <li class="nav-item dropdown dropdown-item">
          <a
            class="nav-link dropdown-bs-toggle hidden-arrow"
            href="#"
            id="navbarDropdown"
            role="button"
            data-bs-toggle="dropdown"
            aria-haspopup="true"
            aria-expanded="false"
          >

            <i class="bi-search"></i>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a
              class="dropdown-item"
              href="/me/{{user.title}}/{{user._id}}/MyProfile"
            >My profile</a>
            <a
              class="dropdown-item"
              href="/me/{{user.title}}/{{user._id}}/MyProfile"
            >My course</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="/">Log out</a>
          </div>
        </li>

        <li class="nav-item dropdown dropdown-item">
          <a
            class="nav-link dropdown-bs-toggle hidden-arrow"
            href="#"
            id="navbarDropdown"
            role="button"
            data-bs-toggle="dropdown"
            aria-haspopup="true"
            aria-expanded="false"
          >

            <img
              src="data:{{user.avatar.contentType}};base64,{{user.avatar.data}}"
              class="img-fluid my-1"
              style="vertical-align: middle;
                      width: 25px;
                      height: 25px;
                      border-radius: 50%;"
            />
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a
              class="dropdown-item"
              href="/me/{{user.title}}/{{user._id}}/MyProfile"
            >My profile</a>
            <a
              class="dropdown-item"
              href="/me/{{user.title}}/{{user._id}}/MyProfile"
            >My course</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="/">Log out</a>
          </div>
        </li>

      </ul>
    </div>
  </div>
</nav>